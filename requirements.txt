# ARIA - Adaptive Resilient Intelligent Autopilot
# Core dependencies (Python >= 3.10). These are sufficient to run the full
# 4-agent simulation, the test suite, and generate every figure:
#     pip install -r requirements.txt
#     python simulation/run_simulation.py
#     pytest tests/
#
# Optional stacks:
#   - Reinforcement learning (Agent A training): requirements-ml.txt
#   - Local LLM (Agent B real parsing):          requirements-ml.txt
#   - CLIP vision guardrails (Agent C):          requirements-ml.txt

# === Scientific core ===
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0

# === Estimation / filtering (Agent D Kalman wind estimator) ===
filterpy>=1.4.5

# === Logging & console output ===
loguru>=0.7.2
rich>=13.6.0

# === Testing ===
pytest>=7.4.0
